Before running the model in mains/main, you need to specify :

    In mains/main:
        - the counties of interest
        - the trial number
        - Select if you want the training process only or producing outcome as well
    In configs/pop_example:
        - number of epochs
        - learing rate
        - batch size
        - chunk size
        - feature list
        - feature values